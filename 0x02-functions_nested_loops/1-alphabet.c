#include <stdio.h>
/**
 * print_alphabet - print alphabet in lower case
 *
 * Return: Always 0 (sucess)
 */
void print_alphabet(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	{
		_putchar('c');
	}
	_putchar('\n');
	return (0);
}
